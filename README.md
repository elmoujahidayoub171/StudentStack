## Overview
This script provisions students from a CSV file. It:
- Cleans/normalizes CSV data (creates safe usernames, teams, lists)
- Creates Linux groups/users
- Generates SSH keys
- Deploys Apache personal + team websites
- Creates MySQL databases and grants access
